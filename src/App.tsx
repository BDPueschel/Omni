export function App() {
  return (
    <div class="omni-container">
      <input
        type="text"
        class="omni-input"
        placeholder="Search files, apps, or type a command..."
        autofocus
      />
    </div>
  );
}
